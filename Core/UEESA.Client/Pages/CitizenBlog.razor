@page "/blog"

<div id="blog-page" class="row justify-content-center">
    <div class="col-auto padded-scroll">

    </div>
</div>

@inject UIState UIState

@code {

    protected override async Task OnInitializedAsync()
    {
        await base.OnInitializedAsync();
        UIState.SetNavBarTickerVisibility(true);
    }
}
